name: my-workflow

on:
  workflow_dispatch:

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: first-step
        run: echo "Hello World"

      - name: second-step
        run: echo "This is the second step"