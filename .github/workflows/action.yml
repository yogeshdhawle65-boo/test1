name: my-workflow

on:
  workflow_dispatch:

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: first-step
        run: echo "Hello World"

      - name: second-step
        uses: actions/checkout@v6

  second-job:
    runs-on: ubuntu-latest

    steps:
      - name: test-step
        run: echo "This is a test step"

      - name: test-code
        uses: actions/checkout@v6